class main{
    String student = "aayushkakshapati";
            int id = 5;
            char c = 'N';  //char allows to print the character .
                    public static void main (String[] arg) {
                    main m1 = new main();
                         System.out.println(m1.id);
                        System.out.println(m1.student.length()); //.length returns the length of the string.
                        System.out.println(m1.student.charAt(2)); //,charAT() gives the character at nth place.
                        System.out.println(m1.student.substring(0,6)); //.substring gives the character from certain part of the string.
                     //lowercase and uppercase
                        System.out.println(m1.student.toLowerCase()); //.to_lowercase prints the given string in lowercase.
                        System.out.println(m1.student.toUpperCase()); //.to_uppercase prints the given string in uppercase.



                     //print the character
                        System.out.println(m1.c);


                    }

}